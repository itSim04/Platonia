import { FriendListPipe } from './friend-list.pipe';

describe('FriendListPipe', () => {
  it('create an instance', () => {
    const pipe = new FriendListPipe();
    expect(pipe).toBeTruthy();
  });
});
